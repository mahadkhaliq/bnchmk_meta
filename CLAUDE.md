# Claude Code Project Guide

Read and follow [`AGENTS.md`](AGENTS.md) before changing this repository. It is
the shared source of project structure, dataset semantics, Lorentz-model
context, commands, and experimental rules.

Project-local Claude permissions are stored in `.claude/settings.local.json`.
They are intentionally narrow and may be revised on the destination computer
after reviewing the new paths and shell environment.

